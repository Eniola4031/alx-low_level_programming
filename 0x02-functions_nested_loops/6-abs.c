#include "main.h"
/**
 * _abs - absolute value of an integer
 * @c: number to be computed
 *
 * Return: always 0
*/
int _abs(int c)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
	_putchar('\n');
}
